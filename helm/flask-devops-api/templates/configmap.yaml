apiVersion: v1
kind: ConfigMap

metadata:
  name: flask-devops-config
  labels:
    {{- include "flask-devops-api.labels" . | nindent 4 }}

data:
  APP_ENV: {{ .Values.config.APP_ENV | quote }}
  APP_LOG_LEVEL: {{ .Values.config.APP_LOG_LEVEL | quote }}
